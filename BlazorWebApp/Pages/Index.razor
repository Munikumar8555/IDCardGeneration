@page "/"
@code {
    protected override void OnInitialized()
    {
        try
        {
            NavigationManager.NavigateTo("/login", true);
        }
        catch (Exception ex)
        {
            // Optionally log or handle navigation error
        }
    }
    [Inject] NavigationManager NavigationManager { get; set; }
}
